#!/bin/bash

# mr wizard get me the hell outta here

if [[ "$0" == "--help" ]] ; then
  echo "Usage: up [count]   # cd [count] directories out (../../../ etc)"
  exit 0
fi

if [[ "$#" < 1 ]] ; then
  cd ..
else
  CDSTR=""
  for i in {1..$1} ; do
    CDSTR="../$CDSTR"
  done
  cd $CDSTR
fi
